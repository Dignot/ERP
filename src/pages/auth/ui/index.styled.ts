import { styled } from "@mui/material/styles";

export const AuthWrapper = styled('div')({
    width: "50%",
    height: "100%",
    display: 'flex',
    alignItems: "center",
    justifyContent: "center",
    flexFlow: 'column',
    gap: '12px'
})